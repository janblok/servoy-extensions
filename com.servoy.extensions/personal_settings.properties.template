# make a copy of this file and remove the ".template" if you need to customise the ant build (such as using a specific compiler instead of the one from JAVA_HOME)
jdk1.7_home = [my_jdk1.7_home_path]
